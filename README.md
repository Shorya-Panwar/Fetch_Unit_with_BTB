# Fetch_Unit_with_BTB
A Fetch Unit which implements a Branch Target Buffer (BTB). The BTB is implemented as a 4-way set associative cache which predicts whether the program control should jump at the next instruction by using a 2-bit saturating counter. This Fetch Unit can be readily integrated with a pipelined processor.
